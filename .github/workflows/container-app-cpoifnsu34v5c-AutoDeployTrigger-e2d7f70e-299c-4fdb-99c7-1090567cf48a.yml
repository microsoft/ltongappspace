name: Trigger auto deployment for container-app-cpoifnsu34v5c

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-cpoifnsu34v5c-AutoDeployTrigger-e2d7f70e-299c-4fdb-99c7-1090567cf48a.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPCPOIFNSU34V5C_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPCPOIFNSU34V5C_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPCPOIFNSU34V5C_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPCPOIFNSU34V5C_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPCPOIFNSU34V5C_REGISTRY_PASSWORD }}
          containerAppName: container-app-cpoifnsu34v5c
          resourceGroup: ltongappspaceb930
          imageToBuild: default/container-app-cpoifnsu34v5c:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


